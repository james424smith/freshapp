export const READ_MORE_NEWS = "ReadMoreNews";
/**
 * In order to get the types work we need to declare the type of the constant as its value as well.
 * (This is a limitation of flow that doesn't accept a constant's value as the type of a variable)
 * So both strings entered in a constant declaration need to be in sync.
 */
export const NEWSLETTERS: "Newsletters" = "Newsletters";
export const PRESS_RELEASES: "PressReleases" = "PressReleases";
